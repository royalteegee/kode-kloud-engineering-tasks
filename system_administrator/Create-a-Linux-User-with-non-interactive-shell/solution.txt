SSH into App Server 2

        ssh steve@stapp02

Check if the user exists

        sudo cat /etc/passwd | grep anita

Run the two commands to confirm the id of anita and the user details

        id anita
        sudo cat /etc/passwd | grep anita